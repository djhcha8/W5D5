class Game {
  run() {
    // 1. initialize with three different arrays,
    // one of them stacked with largest on bottom
    // and smallest on the top
    // 2. until all items are stacked from smallest to
    // largest from the top to the bottm in a different
    // array
      // 3. prompt the user for a move: choosing from and
      // to stacks
      // 4. check if the move is valid
        // 5. if valid make move
        // 6. if not, prompt the user and ask for a different
        // move
    // 7. WIN!!! 
  }
}